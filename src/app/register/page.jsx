import AuthScreen from "@/components/AuthScreen";

export const metadata = {
  title: "Create account | PrimeKey",
};

export default function RegisterPage() {
  return <AuthScreen mode="register" />;
}
